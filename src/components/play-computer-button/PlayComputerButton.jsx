import React  from 'react'
import Button from 'react-bootstrap/Button'
import './style.css'

export const PlayComputer = () => {
    return (
        <Button className='playButton' variant="dark">Play with computer</Button>
    )
}